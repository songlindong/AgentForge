# language: zh-CN
功能: 金融客服可重复验证环境
  为了让后续服务在不连接真实模型和真实金融系统时也能被确定性验证
  作为 AgentForge 开发与质量门禁
  我需要受控的 Fake、Mock、固定样例、场景回放和数据生成器

  场景: Fake LLM 覆盖正常与故障响应
    假如 Fake LLM 只监听 Loopback 并注册了 12 个固定场景
    当调用方选择正常、SSE、工具调用、限流、超时、断流或非法 JSON 场景
    那么服务应返回与场景目录一致的固定结果
    并且完整 SSE 应以 "data: [DONE]" 结束

  场景: Fake LLM 拒绝不受控图片地址
    假如请求中包含图片输入
    当图片地址不是 "agentforge://objects/" 受控引用
    那么 Fake LLM 应以 422 和 VALIDATION_FAILED 拒绝请求

  场景: Mock MCP 优先执行授权与租户校验
    假如 Mock MCP 收到一个工具调用
    当调用缺少 authorization_decision_id 或请求租户与已验证租户不一致
    那么结果状态应为 denied
    并且不得返回利率、额度或审批状态等替代金融数值

  场景: Mock MCP 提供确定性的动态金融数据
    假如调用已通过授权与租户校验
    当调用已注册的虚构贷款工具并选择 success 场景
    那么结果应包含固定数据和 mock=true 的来源信息
    并且不得访问真实征信、银行或资金方接口

  场景: 多模态固定样例可追溯且未被篡改
    假如目录中存在文本 PDF、扫描 PDF、PNG、JPEG、表格和 APP/H5 消息
    当执行 Fixture 校验
    那么每个文件的 SHA-256 应与目录清单一致
    并且黄金标注应包含版本、页码、区域坐标、提取版本和对象来源

  场景: 安全样例保持无害
    假如安全目录只保存小文件、文本和元数据
    当执行 Fixture 校验
    那么目录不得包含可执行恶意程序、压缩炸弹或无限循环程序
    并且每个样例应声明预期拒绝码或隔离结果

  场景: Agent 与 Skill 风险场景可确定性回放
    假如回放目录覆盖 9 个必需风险场景
    当执行全部回放
    那么每个场景的 sequence 应从 0 连续递增
    并且 tenant_id、trace_id 和最终状态应与黄金期望一致

  场景: Checkpoint 恢复不重复副作用
    假如回放中已经记录一个成功的幂等副作用
    当从 Checkpoint 执行一致性回放
    那么同一个 idempotency_key 不得再次产生完成事件

  场景: 固定 Seed 生成结果一致
    假如 Gateway 请求和向量生成器使用相同参数与 Seed
    当分别生成两份 JSONL
    那么两份输出的字节内容应完全相同
    并且生成器不得连接 Gateway、Milvus 或外部服务

  场景: 向量生成器采用流式写出
    假如调用方显式指定数量、维度、租户数和 Seed
    当生成向量 JSONL
    那么生成器应逐行写出并显式包含 tenant_id
    并且默认命令不得意外生成 100 万条数据

  场景: Harness 进入统一离线门禁
    假如仓库已经具备第 6 步 Harness
    当执行统一 CI 检查
    那么 Harness 校验、回放和单元测试应全部通过
    并且检查过程不得调用付费模型或真实金融接口
