# language: zh-CN
功能: 金融多模态知识写入闭环
  为了让金融政策、合同和产品文档形成可追溯且租户隔离的知识版本
  作为知识运营系统
  我需要安全、幂等、可重试并通过双索引门禁的异步入库流程

  场景: 原生 PDF 完成可追溯写入
    假如租户已提交包含文本和表格的固定 PDF
    当入库任务完成解析、切片、Embedding 和双索引写入
    那么原文件应保存于该租户的私有对象路径
    并且每个片段应包含文档版本、页码、区域坐标和提取模型版本

  场景: 扫描 PDF 和图片通过受控 OCR Provider
    假如输入是没有文本层的扫描 PDF、PNG 或 JPEG
    当 Local/Test 入库流程调用 Harness OCR
    那么 OCR 区域应与固定样例哈希对应
    并且结果应标记 test_model=true 且不得用于生产配置

  场景: MIME 与文件魔数不一致时拒绝
    假如文件声明为 JPEG 但文件魔数不是 JPEG
    当系统执行上传安全检查
    那么任务应进入 rejected 状态并返回 UNSUPPORTED_MEDIA_TYPE
    并且不得写入知识片段或索引

  场景: 相同请求幂等返回
    假如同一租户已经使用一个幂等键提交固定文件
    当相同请求摘要再次使用该幂等键
    那么系统应返回原任务和原文档版本
    并且不得创建重复对象、片段或索引记录

  场景: 幂等键冲突被拒绝
    假如同一租户已经使用一个幂等键提交固定文件
    当不同请求摘要再次使用该幂等键
    那么系统应返回 IDEMPOTENCY_CONFLICT
    并且原任务状态不得被覆盖

  场景: 重复事件只消费一次
    假如 Kafka 对相同 event_id 至少投递两次
    当 Consumer 处理事件并写入 Inbox
    那么业务副作用只应执行一次
    并且重复投递应作为成功去重而不是创建脏数据

  场景: 单侧索引失败时禁止发布
    假如 MySQL 和 OpenSearch 已写入全部片段而 Milvus 暂时失败
    当系统校验知识版本发布条件
    那么任务应进入可重试 failed 状态并返回 DEPENDENCY_UNAVAILABLE
    并且不得发布只有单侧索引的知识版本

  场景: 失败重试复用确定性标识
    假如一个索引任务因可重试依赖错误失败
    当任务在最大尝试次数内重新执行
    那么片段和索引应复用原 chunk_uid
    并且双索引数量一致后才能进入 published 状态

  场景: 跨租户操作被拒绝
    假如文档属于租户 A
    当租户 B 查询、重试、覆盖或发布该任务
    那么系统应返回 TENANT_MISMATCH 或资源不可见
    并且跨租户泄漏数量应为 0

  场景: OCR 文本中的指令不能控制系统
    假如文档正文包含要求泄露数据或绕过权限的指令文本
    当解析器保存 OCR 区域并继续状态机
    那么该文本只应作为不可信内容存储
    并且不得改变租户、权限、对象路径或 Provider 配置

  场景: 只有完整双索引才能发布
    假如 MySQL、OpenSearch 和 Milvus 包含相同的全部 chunk_uid
    当系统校验片段数量、Embedding 版本和索引版本
    那么任务可以进入 index_ready 并发布不可变知识版本
    并且发布事件应通过 Transactional Outbox 提交

  场景: 生产配置拒绝测试 Provider
    假如运行 Profile 是 production
    当配置启用 Harness OCR、Hash Embedding 或进程内解析实现
    那么服务启动门禁应失败
    并且不得把测试模型结果发布为生产多模态能力
