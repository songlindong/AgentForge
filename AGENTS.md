# AgentForge 协作规则

本文件适用于整个仓库，供 Codex 及其他开发者在后续任务中遵循。

## 1. 首要原则：一次只推进一个实施步骤

开始工作前必须依次阅读：

1. `docs/project-context.md`
2. `docs/development-roadmap.md`
3. `docs/progress.md`

只能执行 `docs/progress.md` 中标记的“当前步骤”。不得顺手实现下一步骤，不得因为技术上相关而提前搭建后续模块。

完成当前步骤后必须停下，向用户解释本步的原理、关键文件、验证方法和遗留问题；只有用户明确确认已经看懂本步内容后，才能把下一步骤设为当前步骤。

## 2. 不可变核心目标

以下五项是项目永久技术主线，任何后续规划和实现都不得删除、降级、合并后遗漏或用普通业务功能替代：

1. Agent Runtime 内核、Agent 编排与 Skill 体系开发
2. 安全沙箱与隔离环境建设
3. 企业级大模型网关（LLM Gateway）开发
4. 高效记忆系统（Memory）与知识库（Knowledge Base）建设
5. 高并发 LLM 网关、百万级 RAG 知识库优化

金融回答护栏不能替代安全沙箱，普通 HTTP 接口不能替代企业级 LLM Gateway，聊天记录不能替代分层 Memory，简单向量检索不能替代可评测的 Knowledge Base，基础功能可用不能替代高并发和百万向量实测。

Agent 编排必须覆盖 Agent 注册、路由、规划、顺序/并行/条件执行、DAG、人工审批、Checkpoint、恢复、权限和预算控制。Skill 必须是具有 Manifest、输入输出 Schema、版本、权限、依赖、超时/重试策略、实现类型和验收场景的标准能力单元；一段 Prompt、普通函数或未经注册的工具不能直接视为企业级 Skill。

每项核心目标都必须保持“规格 → 实现 → Harness 验证 → 上线前门禁 → 上线后监控”的证据链。五项中任何一项未通过目标环境验收，都不得将项目状态标记为“正式上线完成”。

## 3. SDD：先规格，后代码

任何模块编码前，都必须先写清：

- 目标与非目标
- 用户场景
- 输入、输出与错误语义
- 核心流程和状态变化
- Agent 编排关系、Skill Manifest 与生命周期
- 安全不变量与多租户边界
- SLO 或可度量指标
- Given/When/Then 验收场景

接口按需使用 OpenAPI，工具参数使用 JSON Schema，Kafka 事件使用 AsyncAPI，重要架构选择使用 ADR。规格与实现必须放在同一个提交上下文中保持一致。

## 4. Harness：每项能力必须可验证

实现不能只证明“能够运行”，还要有与风险相匹配的验证证据。按阶段逐步建立：

- Fake LLM：固定响应、SSE、延迟、429、5xx、超时、断流、工具调用和无效 JSON
- Mock MCP：确定性的正常、失败、超时和越权场景
- Skill Registry、版本兼容、权限、依赖、组合执行和回滚测试
- 单元、契约、集成、端到端测试
- 固定 RAG 文档、黄金问题与检索基线
- Agent 场景回放、安全测试与 k6 压测
- Trace、指标和可复现实验报告

失败测试不得通过删除断言、扩大容差或跳过场景来掩盖；需要先说明根因。

## 5. 技术与安全约束

- 关系型数据库固定使用 MySQL 8.0+，除非用户明确批准新的 ADR。
- 所有业务数据访问都必须显式包含 `tenant_id`；Harness 必须验证租户 A 无法读写租户 B 数据。
- 向量检索使用 Milvus，BM25 使用 OpenSearch；不让 MySQL 承担向量或全文检索职责。
- 贷款产品政策、合同条款和合规制度必须能够追溯到知识来源；利率、额度、征信和审批状态等动态信息必须通过受控 MCP/业务接口获取，禁止由模型臆测。
- 默认只使用虚构、合成或脱敏的金融数据；未经合法授权，不接入真实征信、银行或资金方生产接口。
- 不提交密码、Token、私钥、身份证号、手机号、银行卡号或真实个人/生产数据；示例配置只使用安全占位符，日志必须脱敏。
- 不可信代码、租户脚本、高风险本地工具和需要隔离的计算任务必须进入安全沙箱。第一版沙箱至少具备 non-root、只读根文件系统、默认禁网、CPU/内存/PID 限制、执行超时、临时目录、禁止 Docker Socket 和任务结束销毁。
- 所有 Skill 必须通过 Skill Registry 注册后才能被 Agent 调用，并显式声明租户可见性、权限、输入输出 Schema、版本和执行边界。Skill 不得绕过 LLM Gateway、租户校验、MCP 授权或安全沙箱直接访问受限资源。
- Agent 编排必须设置最大步数、截止时间、Token/费用预算、并发上限和人工审批点，并为每个 Agent、Skill 和工具调用记录 trace_id。
- 第一版服务器发布使用 Linux + Docker Compose + HTTPS；不得提前迁移 Kubernetes。
- 生产发布步骤必须验证域名与 TLS、密钥管理、数据库迁移、备份恢复、健康检查、监控告警、灰度或滚动发布和回滚，不能只以“容器已启动”作为上线完成标准。
- 外部模型与第三方服务必须可由本地 Fake/Mock 替代，核心测试不能依赖付费 API。

## 6. 变更纪律

- 保持改动小而可审查，不做与当前步骤无关的重构。
- 不覆盖用户已有改动；发现冲突时先说明。
- 新依赖必须说明用途、替代方案和引入成本。
- 代码生成物和本地数据必须加入忽略规则，不能污染仓库。
- 每步结束都要更新 `docs/progress.md`；架构决策变化时同步更新项目上下文或新增 ADR。

## 7. 每步交付说明

每次交付至少回答：

1. 本步完成了什么？
2. 为什么这样设计？
3. 哪些文件最值得阅读？
4. 如何运行和验证？
5. 当前还没有做什么？
6. 是否已经到达暂停点？
